services:
  backend:
    build: ./backend
    container_name: ki-email-backend
    restart: unless-stopped
    env_file: .env
    networks:
      - 1panel-network

  frontend:
    build: ./frontend
    container_name: ki-email-frontend
    restart: unless-stopped
    depends_on:
      - backend
    networks:
      - 1panel-network

networks:
  1panel-network:
    external: true
